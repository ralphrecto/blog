---
layout: base.liquid
title: blog
---
<ul>
  {%- for post in collections.posts reversed -%}
    {% if post.data.title != "blog" %}
    <h6>
      <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    </h6>
    {% endif %}
  {%- endfor -%}
</ul>